---
- name: monitor Postfix
  template:
    src: datadog/postfix.yaml.j2
    dest: "{{ datadog_configs_dir }}/postfix.yaml"
    owner: "{{ datadog_user }}"
    group: "{{ datadog_group }}"
    mode: 0644
  notify:
    - restart Datadog
