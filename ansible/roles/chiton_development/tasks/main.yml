---
- name: load the environment settings
  include_vars: environment.yml
  no_log: yes

- name: configure the application environment
  include: environment.yml
  become: yes
  become_user: "{{ chiton_user }}"

- name: install dependencies
  include: dependencies.yml
  become: yes
  become_user: "{{ chiton_user }}"
