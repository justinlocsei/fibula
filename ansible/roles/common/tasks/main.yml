---
- name: ensure that ntp is current
  apt:
    name: ntp
    state: latest
  notify:
    - restart ntp

- name: ensure that ntp is running
  service:
    name: ntp
    state: running
    enabled: yes

- name: customize readline settings
  copy:
    src: inputrc
    dest: /etc/inputrc
    owner: root
    group: root
    mode: 0644
