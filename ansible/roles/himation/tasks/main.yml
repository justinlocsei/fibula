---
- name: create the Himation group
  group:
    name: "{{ himation_group }}"
    state: present

- name: create the Himation user
  user:
    name: "{{ himation_user }}"
    group: "{{ himation_group }}"
    comment: "Himation application user"
    state: present

- name: fetch the Himation source code
  git:
    accept_hostkey: yes
    clone: yes
    dest: "{{ himation_source_dir }}"
    repo: git@github.com:justinlocsei/himation.git
    update: yes
    version: "{{ himation_git_branch }}"
